.model small
.STACK 100h
.DATA
    FI DB 'IUDIN ARTYOM' ;name of string
.CODE
START:

    mov AH, 2 ; DOS command of output
    mov BH, 0 ; ????? ????????
    mov DH, 13 ; line
    mov DL, 26 ; symbol of line

    int 10h ; stopping BIOS 10h ??????????


    mov ax, @data ; ES:BP => ????????? ??????
    mov es, ax ; ? ES ??????? ??????
    lea bp, [FI] ; ? BP ????? ??????
    mov cx, 16 ; ????? ??????
    mov bl, 14 ; color 
    mov ah, 13h ; error number in ah ? ah, ???? ??? ???? 13h
    mov al, 0 ; codding in ASCII? BL; ?? ??????? ??????)

    int 10h ; stopping BIOS 10h ??????????

END START
